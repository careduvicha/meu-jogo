var canvas;
var form 
var game
var player
function preload() {
  
}

function setup() {
  canvas = createCanvas(windowWidth, windowHeight);
  form=new Form()
  form.setElementsConfig()
}

function draw() {
  
}

function windowResized() {
  resizeCanvas(windowWidth, windowHeight);
}
